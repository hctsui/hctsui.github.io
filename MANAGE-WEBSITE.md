# 網站與雙語履歷管理說明

日常入口：<https://hctsui.github.io/admin/>

## 核心資料結構

網站採用「頁面 → 類別 → 項目」三層結構：

- **頁面**決定導覽位置、網址、頁首與主色。
- **類別**就是頁面中的大區塊；類別的「顯示風格」決定其中項目的網站與 PDF 排版。
- **項目**必須屬於一個類別。類別移到另一頁時，其中項目會一起移動。

所有修改先保存在目前瀏覽器的草稿中；建立 GitHub Issue 並由自動流程成功套用後，才會改動正式資料。

## 項目清單

「項目」分頁的每筆資料會顯示新版提示標籤：

- 項目類型。
- 所在頁面。
- 所在類別；頁面項目則顯示語言版本。

搜尋、篩選項目類型，以及切換「網站順序／日期／名稱」時，都會維持相同的新版標籤，不會退回舊式單一標籤。

項目類型篩選只保留一個「一般內容」選項，會同時顯示標準時間軸、雙欄紀錄、標題清單、資訊卡片與標籤列表等一般內容資料；這只合併篩選入口，不會改變各項目的實際顯示風格。榮譽與四種活動仍保留獨立篩選。

## 一般內容的六種顯示風格

一般內容新增與編輯時都可選擇顯示風格。風格只描述排版，不限制內容用途；儲存時必須選擇一個使用相同風格的類別。

| 顯示風格 | 排版概念 | 常見用途範例 |
| --- | --- | --- |
| 標準時間軸 | 日期在左，標題、機構與說明在右 | 一般經歷、服務紀錄 |
| 雙欄紀錄 | 日期欄與內容欄分明，內容採分層顯示 | 學歷、長期職務 |
| 標題清單 | 標題為主，補充文字在下一行 | 研究方向、主題清單 |
| 資訊卡片 | 每筆內容形成獨立卡片 | 聯絡方式、重點資訊 |
| 標籤列表 | 短標籤與內容並列 | 語言、技能、個人資料 |
| 精簡時間軸 | 年份與短內容壓縮在一行 | 榮譽、獎項、簡短里程碑 |

表單內有六種風格的即時縮圖；選擇風格後，符合的類別才會出現在「所屬類別」選單。右側預覽會明確顯示修改前後的風格與類別。編輯既有項目時也能改風格；若跨越榮譽與其他一般內容的資料區塊，Admin 會安全地建立成對的刪除／新增草稿，並由同一批次送出。風格變更自動產生的類別／排序草稿會與內容草稿連動，刪除草稿或改回原風格與類別時會一起還原。

## 統一排序與搬移

「排序」依頁面顯示所有類別與項目：

- 調整同一頁面中類別的順序。
- 調整類別內項目的順序。
- 在單一類別內依日期新到舊、日期舊到新或名稱快速排序。
- 每個項目都顯示「搬移」選單；選單列出所有能接受該項目格式的類別。
- 沒有其他相容類別時，「搬移」選單仍會顯示，但呈停用狀態。
- 「PDF 履歷」檢視只調整履歷類別順序，因此項目搬移選單停用。

「搬移」可以跨頁面；項目會跟著目標類別出現在新的頁面與 PDF 區段。



## 首頁內容

「項目」中的頁面清單會顯示「首頁」。編輯首頁時，表單只開放首頁最上方的五行文字與按鈕清單：

- 系所／學校第一行（中英文）。
- 英文姓名。
- 中文姓名。
- 身分文字（中英文）。
- 說明文字（中英文）。
- 首頁按鈕的中英文文字與 URL。

首頁按鈕可以新增或刪除，排列順序就是網站顯示順序。照片、照片說明、精選論文、近期活動及聯絡區不在這個表單修改，仍由原本的網站資料與排序功能管理。

首頁內容會以內部管理項目保存，因此沿用既有的草稿、預覽、衝突檢查、修改歷史與還原機制；該內部項目不會出現在一般項目清單或公開網站類別中。


## 首頁精選與近期活動

在「排序」選擇「首頁」後，「精選論文」與「近期活動」會直接在原本的首頁類別卡中顯示各自的設定與排序清單，不再另外放獨立區塊或空項目區；卡片和項目列沿用其他排序類別的視覺風格。

- 精選論文：最新 N 篇、最舊 N 篇，或手動選擇。
- 近期活動：最近 N 筆、日期最遠 N 筆，或手動選擇。
- 自動模式先依日期決定入選的 N 筆，再套用管理者調整的相對順序；新資料進入前 N 筆時，候選集合仍會自動更新。
- 手動模式可自行加入、移除及排序。
- 操作提示使用欄位下方的小字，不使用佔版面的說明卡。
- 每個首頁區塊以小標籤顯示「目前無變更」或「有未送出變更」。
- 變更先保存為本機草稿；首頁草稿會列入「草稿」分頁，可修改、預覽或單獨移除。

## 頁面與類別

頁面與類別都從「新增」建立，也能在「項目」搜尋與編輯。

- 頁面可設為雙語、僅中文或僅英文；建立後語言版本固定。
- 類別可編輯中英文小字、大標題、簡介、所在頁面，以及是否顯示於網站或 PDF。
- 類別顯示風格建立後固定；需要另一種排版時新增相應風格類別，再搬移項目。
- 刪除含項目的類別時，必須先把項目移到同格式的其他類別。

## Conference 與 Organization

學術會議與學術籌辦是兩種完全獨立的項目，不會自動複製或連動。需要同一活動同時出現在兩區時，建立兩筆資料。

## 中英文網站與 PDF 履歷

同一份 `content/site.json` 產生英文網站、中文網站、英文 PDF 履歷與中文 PDF 履歷。類別標題、項目所屬類別與排序會同步到 PDF；只有勾選「顯示於 PDF 履歷」的類別會輸出。

中文履歷標題及題目支援：

- `$t$` 或 `\(t\)`：數學模式。
- `[i]t[/i]` 或 `<em>t</em>`：斜體／數學變數。

## 學術訪問 Funding

學術訪問可填資助機構與資助計畫。開啟自動生成說明後，系統會分別產生對應語言的資助句子，且不會被一般的中英補填覆蓋。

## 資料庫：中英對照與人名連結

Admin 的「資料庫」分成兩個子類型，切換按鈕與上方主分頁之間保留獨立間距：

- **中英對照**：新增、修改、刪除、合併與排序詞條和標籤，也可匯入／匯出 JSON。大型字詞庫會自動 gzip 壓縮，避免 GitHub Issue 欄位長度限制。
- **人名連結**：集中管理英文姓名、中文姓名、其他拼法與學術網址。其他拼法每行一個，例如 `Tsui, Hung-Chun`。
- 編輯論文作者時，輸入框下方只會列出可能的人名；必須手動點選才會填入，不會因相似姓名自動替換。
- 人名精確比對會套用到整個公開網站，包括首頁說明、指導教授、活動、教學及論文作者。既有超連結、粗體、程式碼、數學標記與 BibTeX 不會被重複包覆。
- 人名資料保存於 `content/people.json`；沒有匹配或沒有網址時維持普通文字，PDF CV 不加入人名連結。

## 論文 BibTeX 與 arXiv 通知

- 公開網站的 BibTeX 使用和 arXiv、PDF 相同的膠囊按鈕樣式；點擊後可在 **BibTeX** 與 **LaTeX `\bibitem`** 間切換，兩種格式都有獨立複製按鈕。
- 論文表單可分別手動覆寫 BibTeX 與 `\bibitem`；任一欄留白時，網站會依作者、題目、年份、arXiv／DOI 自動產生。
- `.github/workflows/check-arxiv.yml` 每週一上午 9:15（Asia/Tokyo）執行，也可從 Actions 手動執行。
- `tools/check_arxiv.py` 使用 arXiv 官方 API 搜尋 `Hung-Chun Tsui`，再以完整作者姓名做第二次精確比對，避免只因姓氏或相似姓名而產生通知。
- 新結果保存在 `content/arxiv-suggestions.json`，不會直接寫入公開論文列表。
- Admin 頁首的「通知」按鈕位於「開啟完整使用手冊」左側，數字表示目前尚未處理的候選論文。
- 按「加入新增草稿」只會建立一般論文草稿並開啟編輯表單；請確認中文題目、中文作者、PDF 與正式刊登資訊後，再和其他草稿一起送出。
- 按「忽略」只會先加入本機忽略草稿；送出 Batch Issue 後才會永久寫入忽略清單。忽略操作同樣支援衝突檢查與七日還原。
- 已存在於網站或已加入本機論文草稿的 arXiv ID 不會重複顯示。

## 網站設定

「網站設定」依序分成 **頁尾 → SEO／OG → 流量統計 → 404 頁面**。四個區塊都沿用本機草稿、右側逐欄預覽、Batch Issue、衝突檢查與七日還原。

### 頁尾

- 頁尾可新增多個中英文項目，設定超連結、是否開新分頁、靠左／置中／靠右及上下順序。
- 預覽分成英文與中文兩列，使用固定高對比配色，不會因公開網站的頁尾顏色而看不清楚。
- 小圖標選單順序為：無圖標、版權、連結、地點、書籍、日曆、其他。
- 選擇「其他」時，必須填入已放在 repository 中的圖檔相對路徑，例如 `assets/icons/my-icon.svg`；也可使用完整 `https://` 網址。
- 頁尾文字支援 `{year}` 與 `{updated}`，分別代入目前年份和網站最後更新日期。

### SEO／OG

- 可設定基準網址、網站名稱、預設分享圖片，以及每頁中英文 SEO 標題、Meta description、OG 標題、OG 描述與個別分享圖片。
- 建置時自動產生 canonical、Open Graph、Twitter Card、中文／英文 hreflang 與 x-default。

### Cloudflare Web Analytics

- 流量統計預設關閉；啟用後填入 Cloudflare Web Analytics 的 32 字元 Site Token。
- 產生器只把 Cloudflare beacon 加入公開頁面與 `404.html`，不會加入 `/admin/`。
- 關閉功能或清空 Token 後，下一次建置會移除先前由系統加入的 beacon。

### 404 頁面

- GitHub Pages 使用 repository 根目錄的 `404.html` 顯示自訂錯誤頁面。
- 可編輯中英文小標、標題、說明、首頁按鈕、次要按鈕與次要網址。
- 可選擇是否顯示導覽列、共用頁尾，以及是否在 1–300 秒後自動返回相同語言的首頁。
- 可調整背景、卡片、強調色、主要文字、說明文字、按鈕及按鈕文字顏色，Admin 會即時預覽。
- 404 頁固定輸出 `noindex,nofollow`，不會被當作正常內容建立索引。

### 網站設定草稿與預覽

- 草稿採正規化後的語意比較；欄位改回原值時會立即恢復「尚未修改」，並自動清除對應的本機草稿。
- 右側預覽不再只顯示「0 個頁面變更」或「2 → 2 個項目」，而會逐項列出真正變動的欄位、修改前值與修改後值。

## GitHub Issue 與自動流程

所有內容修改都從 Admin 送出。Admin 會建立 `batch-changes.yml` 的批次 Issue，並由 `process-website-batch.yml` 套用資料、重建中英文網站與兩份 CV。舊的單筆新增、編輯與排序 Issue Forms 已停用，避免兩套入口產生不一致資料。

每日自動流程只重建日期相關內容；部署流程負責執行測試、重建網站、編譯兩份 PDF CV 並發布 GitHub Pages。

## 主要檔案

- `content/site.json`：頁面、類別、項目與排序資料。
- `content/translations.json`：中英字詞與標籤。
- `content/people.json`：中英文姓名、其他拼法與學術網址。
- `content/arxiv-suggestions.json`：arXiv 查詢設定、待處理候選與忽略清單。
- `404.html`：由網站設定產生的雙語自訂錯誤頁。
- `admin/index.html`：內容編輯介面。
- `admin/layout.js`：頁面、類別與統一排序管理。
- `admin/people.js`：資料庫的人名連結管理與作者候選建議。
- `admin/arxiv-notifications.js`：頁首論文通知、加入草稿與忽略操作。
- `admin/site-settings.js`：頁尾、SEO／OG、Cloudflare 流量統計與 404 管理。
- `admin/homepage.js`：首頁雙欄、一般內容風格編輯與 Admin 相容修正。
- `admin/guide.html`：完整操作手冊。
- `tools/process_batch_request.py`：批次套用、衝突檢查與七日還原。
- `tools/build_site.py`：中英文網站、頁尾、SEO／OG、分析 beacon 與 404 生成。
- `tools/check_arxiv.py`：查詢 arXiv 官方 API 並更新候選通知。
- `tools/site_settings_config.py`：網站設定的預設值、正規化與驗證。
- `tools/build_cv.py`：中英文 LaTeX 履歷生成。

### 活動入口與首頁精選

「新增」中的活動入口統一為「活動」，再選擇學術會議、學術報告、學術訪問或學術籌辦。進入表單後可用「返回活動類型」重新選擇。首頁精選與近期活動直接融入「排序」中的首頁類別位置。「項目」頁的篩選仍保留四種獨立類型，不會合併成單一「活動」篩選。

## PDF 履歷的群組子標題

作品類別與教學機構共用 `\cvgroup` 樣式。主區段標題維持較粗的完整橫線；群組子標題改用較小字級與較淡、較細的右側橫線；橫線以一般水平填滿線繪製，確保在實際 CV 中可見，形成清楚的第二層層級。

因此目前的 Preprints 與 National Tsing Hua University，以及之後新增的 Journal Articles、其他作品類別或其他教學學校，都會自動使用相同格式，不需要逐一修改模板。

## 排序重設與草稿預覽

- 「排序」中每個類別卡都有「重設」。一般類別會還原該類別的位置、項目順序與搬移結果；首頁精選論文與近期活動則各自重設，不會互相影響。
- 首頁變更狀態依實際模式、N 值和顯示順序判斷。若操作後回到原始結果，狀態會自動恢復為「目前無變更」。
- 頁面、類別、項目排序與搬移會在「草稿」分頁顯示一筆「頁面／類別／排序」管理列，可回到排序修改、開啟詳細預覽或重設整組草稿。
- 右側預覽會逐欄列出頁面與類別的修改前後值，並用相同卡片樣式顯示首頁順序、一般內容風格、項目搬移及 PDF 履歷類別順序。預覽右上角有與其他操作一致的「關閉」按鈕，只關閉畫面而不影響草稿。
- 類別用途說明採小字提示，避免占用表單空間。


## URL 與電子郵件連結

一般網址使用 `http://` 或 `https://`；電子郵件項目的 URL 可填 `mailto:name@example.com`。Admin 會保留其他網址驗證，只額外允許格式正確的 `mailto:`。


## Admin 導覽與管理圖示

Admin 頁首使用專用管理圖示作為分頁 favicon 與標題圖示。「開啟完整使用手冊」右側提供「返回網站」按鈕，可直接回到網站首頁。一般內容的顯示風格可直接點選卡片；六張卡片，卡片區仍可用左側箭頭收合。

Admin 外框會在 `admin/homepage.js` 載入時優先建立；即使後續管理功能發生錯誤，favicon、標題圖示與「返回網站」仍會先出現。

## 通知中心與自動檢查

Admin 頂部的「通知」按鈕位於完整使用手冊左側。通知中心整合：

- arXiv 新論文候選：每週一 09:15（Asia/Tokyo）查詢；加入草稿或忽略都由你決定。
- 正式出版候選：每週三 09:35 以 Crossref 比對尚無 DOI 的 Preprints。按「轉為 Published」只會建立修改草稿，仍需人工確認期刊、卷期、頁碼與 DOI。
- 外部連結失效：每月 1 日 10:20 檢查管理資料中的外部網址；只對確認回傳 404/410 的網址建立通知，暫時性網路錯誤、403、429 或 5xx 不直接判定為死鏈。
- 聯絡表單：完整留言只寄到 Email。若啟用 Cloudflare Worker 橋接，Admin 僅保存匿名事件 ID 與收到時間，不保存訪客個資或留言內容。
- 部署狀態：通知面板開啟時讀取 GitHub 公開 Actions 狀態，顯示 ⏳／✅／❌。失敗時可開啟 Log 與 GitHub 重新執行頁面。

通知可搜尋、加星號、標記已讀、刪除或標記已處理。所有狀態調整先成為本機草稿，與其他修改一起透過 Batch Issue 送出，並支援七日 Undo。每週清理排程會刪除超過 60 天且未加星號的通知；加星號的通知不會被自動清除。

### 為什麼不能在 Admin 直接重新執行部署？

重新執行 GitHub Actions 需要 Actions write 權限。Admin 是公開靜態頁面，不能安全存放這種 token，因此只提供「前往 GitHub 重新執行」按鈕。

## 聯絡表單

位置：`網站設定 → 聯絡表單`。表單顯示在首頁聯絡區，支援中英文文字設定。

### Web3Forms Email 模式

填入 Web3Forms Access Key 後即可使用。訊息直接寄到 Email，不在 Admin 建立通知。

### Cloudflare Worker 橋接模式

此模式會在寄信成功後，以匿名事件建立 Admin 通知。完整設定步驟請看 `integrations/CONTACT-FORM-SETUP.md`。

安全原則：

- `WEB3FORMS_ACCESS_KEY`、`GITHUB_TOKEN`、`TURNSTILE_SECRET` 只放 Cloudflare Worker Secrets。
- GitHub token 只授予此 repository 的 Contents write，用於觸發 `repository_dispatch`。
- 公開 repository 永遠不保存訪客姓名、Email、主旨或留言全文。
- 建議啟用 Cloudflare Turnstile；Site Key 可填入 Admin，Secret Key 不可填入 Admin。

## 流量統計

位置：`網站設定 → 流量統計`。可選 Cloudflare Web Analytics 或 Google Analytics 4，一次只啟用一個。既有 Cloudflare Site Token 會自動沿用；GA4 使用 `G-...` Measurement ID。

Admin 不直接顯示訪客統計數字，因為讀取 Cloudflare／Google 報表需要私密 API 憑證，而 Admin 是公開靜態頁面。請使用該分頁的按鈕開啟官方儀表板。

## 人名自動補齊與顯示

一般雙語欄位在啟用「自動填寫另一語言」時，會先精確查詢 `資料庫 → 人名連結`，再查一般中英對照。只有完整姓名或別名唯一匹配時才會補入另一語言，因此不必在兩種資料庫重複建立同一個人。

公開網站的人名連結使用一般字重與 1px 細底線；論文中網站主人的 `<strong>` 粗體仍會保留，讓作者重點清楚。

## 引用格式

公開論文頁的 arXiv、PDF、BibTeX 使用同一款按鈕。展開 BibTeX 後可切換 BibTeX 或 LaTeX `\\bibitem`，並分別複製。自動引用 key 採作者姓氏首字母大寫連接＋年份後兩碼，例如 `T26`、`CCHT25`；同組作者同一年多篇時依序加 `a`、`b`、`c`。
