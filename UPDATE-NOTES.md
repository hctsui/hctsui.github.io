# Admin 修正更新包

基底版本：目前 `cms`（包含 2026-07-31 已上傳的 v4 修正）。

將本壓縮檔內容解壓到 repository 根目錄並覆蓋同名檔案。Admin 與手冊檔案會直接更新；CV 模板因為不應整份覆蓋，請再執行一次安全更新腳本：

```bash
python3 apply-update.py
```

本包主要檔案：

- `admin/homepage-v1.js`
- `admin/guide.html`
- `tools/homepage_config.py`
- `MANAGE-WEBSITE.md`
- `apply-update.py`（只修改四份 CV TeX 中共用的 `\cvgroup` 樣式，可重複執行）

## 修正內容

1. 首頁精選併入「排序」後，改用局部更新與明確草稿狀態，避免被排序頁重繪干擾。
2. 「項目」的搜尋、項目類型篩選與檢視排序，統一呼叫新版標籤渲染，不再退回舊標籤。
3. 一般內容新增與編輯都可選六種顯示風格，表單內附即時版面縮圖。
4. 每個排序項目都顯示「搬移」選單；沒有相容類別時保持顯示但停用。
5. 跨越「精簡時間軸（榮譽）」與其他一般內容格式時，以安全的刪除＋新增草稿處理，同一批次送出。
6. 更新 Admin HTML 手冊與 repository 管理說明。
7. 補回既有測試要求的完整手冊章節：標準工作流程、欄位與自動填寫、排序／頁面／類別、送出前檢查及還原。

## 已執行檢查

- `node --check admin/homepage-v1.js`
- Node VM smoke test：新版搜尋／篩選事件綁定、停用搬移選單輸出。
- `python3 -m unittest discover -s tests`（更新包內測試通過）
- `tests/test_cms.py` 的手冊固定章節字串檢查：通過

GitHub connector 可讀取 repository，但建立工作分支仍回傳 `403 Resource not accessible by integration`，因此此包尚未由助理推送至 GitHub。

## 2026-07-31 第二次修正

- 首頁精選與近期活動改為「排序」頁內固定顯示，不再因排序重繪後收合而看似消失。
- 「新增項目類型」將學術會議、學術報告、學術訪問、學術籌辦合併成「活動」入口，進入後四選一。
- 四種活動表單新增「返回活動類型」按鈕。
- 原項目類型「學術活動」更名為「學術籌辦」。
- 排序列改為左側長標題自行換行、右側上下箭頭與搬移選單不換行。

- 「項目」頁的項目類型篩選仍保留四個獨立選項：學術會議、學術報告、學術訪問、學術籌辦。

## 首頁排序整合修正

- 在「排序」選擇首頁時，精選論文與近期活動直接取代原本的空類別內容。
- 自動模式仍依日期選出 N 筆，但入選後可手動調整相對順序。
- 網站產生端同步套用自動候選集合內的手動順序，避免 Admin 預覽與正式首頁不同。
- 首頁操作說明改為精簡小字，不再使用佔版面的提示卡或一次性更新文字。
- 手冊與 repository 管理說明已同步更新。

## 首頁排序整合 v4

- 「排序 → 首頁」中的「首頁精選論文」與「首頁近期活動」不再顯示空項目區，而是直接承載完整首頁選取與排序介面。
- 固定最新／最舊 N 篇與最近／最遠 N 筆也改用「目前顯示順序」項目列，可用上移、下移調整。
- 自動模式仍依日期更新入選集合；網站產生端會在該集合內套用管理者保存的相對順序。
- 首頁類別卡與一般排序類別共用標題、項目列、按鈕與間距風格。
- 大型提示卡已移除；長期操作說明改為欄位下方小字。
- 每個首頁區塊保留小型狀態標籤：「目前無變更」或「有未送出變更」。
- 完整手冊已同步更新。

## CV 子標題與一般內容篩選 v5

- 英文與中文 CV 的作品類別、教學機構統一使用「較小副標題＋淡色右側橫線」。
- 主 section 的粗線維持不變，子標題橫線使用較淡、較細的樣式，以保留清楚層級。
- 此樣式由共用 `\cvgroup` 控制；未來新增期刊論文等作品類別或其他教學學校會自動套用。
- Admin「項目類型」篩選把多個一般內容格式合併成單一「一般內容」選項；底層顯示風格與資料類型不變。
- 四種活動與榮譽仍保持獨立篩選。
- `apply-update.py` 會安全更新英文／中文 CV 模板與目前產生出的 TeX 檔，可重複執行。


## 排序重設、草稿管理與統一預覽 v7

- 首頁精選論文與近期活動各自加入「重設」按鈕，按鈕名稱統一為「重設」。
- 所有一般排序類別與 PDF 履歷類別卡也加入「重設」，可還原該類別位置、項目順序與搬移結果。
- 首頁草稿改用實際顯示結果判斷差異；調整後完整改回原狀時會自動清除假變更狀態。
- 「草稿」分頁新增「頁面／類別／排序」管理列，可修改、預覽或重設排序與搬移草稿。
- 右側預覽統一為卡片式修改前／修改後版面；首頁順序、頁面欄位、類別欄位、項目搬移與 PDF 履歷順序都採相同視覺語言。
- 頁面與類別預覽會逐欄顯示真正修改內容，不再只顯示類別名稱或概略統計。
- 類別表單的用途提示縮成一般輔助小字，不再佔用大型提示框。
- 「清空內容草稿」同步清除內容、排序、首頁精選與還原草稿。
- 手冊已同步更新到此版本。


## 風格草稿同步與預覽關閉 v8

- 一般內容修改顯示風格時，右側預覽明確顯示「修改前風格 → 修改後風格」與所屬類別變更。
- 風格變更自動產生的類別／排序草稿與內容草稿建立連動；刪除內容草稿或把風格與類別改回原狀時，對應排序變更會同步還原。
- 所有開啟在右側的單筆預覽右上角加入小型「關閉」按鈕；關閉預覽不會修改或刪除草稿。
- 手冊與網站管理說明已同步更新。

## 風格卡片選擇、網站返回與管理圖示 v9

- 一般內容的六張顯示風格預覽改為原生按鈕，直接點卡片即可選擇風格。
- 原本用於資料同步的選單改為隱藏欄位，不再占用表單版面。
- 選中卡片會顯示勾選、強調邊框與鍵盤焦點；區塊標題會顯示目前風格。
- 風格卡片區仍使用 `<details>`，保留左側收合箭頭。
- Admin 頁首在「開啟完整使用手冊」旁加入「返回網站」按鈕。
- 新增 `admin/admin-icon.svg`，用於 Admin 分頁 favicon、主標題圖示與使用手冊 favicon。
- `apply-update.py` 會安全且可重複地更新 `admin/index.html`，加入圖示、返回網站按鈕與必要樣式。
- 完整使用手冊與 repository 管理說明已同步更新。


## 預覽按鈕、mailto 與 CV 橫線 v10

- 預覽右上角「關閉」改用一般 `.button` 尺寸，不再縮成小膠囊。
- URL 欄位額外允許格式正確的 `mailto:`，電子郵件項目可正常編輯。
- 修正 v9 CV 子標題使用 `\titlerule` 在一般段落中可能不顯示的問題。
- CV 子標題改用 `\leaders\hrule ... \hfill`，已用 LaTeX 實際編譯確認 Preprints 與 National Tsing Hua University 右側都有淡色橫線。
- 更新腳本可從舊版無橫線或 v9 未顯示橫線的巨集安全升級，並同步更新英文、中文模板與目前產生的 TeX。
- 手冊與網站管理說明已同步更新。

## v11：修正混包與 CI 失敗

- 根目錄 `apply-update.py` 明確標記為 v11。
- 腳本啟動時會檢查 CV 可見橫線與 `mailto:` 修正是否真的存在；若更新包混入舊腳本，會立即停止並顯示錯誤。
- CV 子標題使用 `\leaders\hrule height 0.45pt\hfill\kern0pt`，不再使用可能無法顯示的 `\titlerule`。
- URL 驗證保留 `http:`、`https:`，並允許格式正確的 `mailto:`。

