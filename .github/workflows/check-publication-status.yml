name: Check publication status

on:
  schedule:
    - cron: "35 9 * * 3"
      timezone: "Asia/Tokyo"
  workflow_dispatch:

concurrency:
  group: hctsui-automation-writes
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out cms branch
        uses: actions/checkout@v6
        with:
          ref: refs/heads/cms
          fetch-depth: 0
          clean: true

      - name: Query Crossref for published versions
        run: python3 tools/check_publication_status.py

      - name: Validate notification data
        run: python3 -m unittest discover -s tests

      - name: Commit notifications
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- content/notifications.json; then
            echo "No publication-status notification changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add content/notifications.json
          git commit -m "Update publication status notifications"
          git pull --rebase origin cms
          git push origin HEAD:cms
